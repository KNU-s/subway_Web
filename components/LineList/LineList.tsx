const LineList = () => {
  // 모든 노선 가져오기
  return (
    <div className="line-list">
      {/* {lineList.map((line) => (
          <LineItem key={line.uniqueLineId} line={line} />
        ))} */}
    </div>
  );
};

export default LineList;
